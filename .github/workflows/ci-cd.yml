# name: CI/CD Pipeline

# on:
#   push:
#     branches: [ main, develop ]
#   pull_request:
#     branches: [ main, develop ]

# env:
#   REGISTRY: docker.io
#   DJANGO_IMAGE_NAME: huynhduc0/ai-testops
#   CONSUMER_IMAGE_NAME: huynhduc0/ai-testops-consumer

# jobs:
#   test:
#     runs-on: ubuntu-latest
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4
    
#     - name: Set up Python
#       uses: actions/setup-python@v5
#       with:
#         python-version: '3.11'
    
#     - name: Cache pip dependencies
#       uses: actions/cache@v4
#       with:
#         path: ~/.cache/pip
#         key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
#         restore-keys: |
#           ${{ runner.os }}-pip-
    
#     - name: Install dependencies
#       run: |
#         python -m pip install --upgrade pip
#         pip install -r requirements.txt
#         pip install pytest pytest-cov pytest-django
    
#     - name: Run tests
#       run: |
#         python manage.py test
#       env:
#         POSTGRES_DB: test_db
#         POSTGRES_USER: test_user
#         POSTGRES_PASSWORD: test_password
#         GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
    
#     - name: Run linting
#       run: |
#         pip install flake8
#         flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
#         flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

#   build-and-push:
#     needs: test
#     runs-on: ubuntu-latest
#     if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
#     permissions:
#       contents: read
#       packages: write
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4
    
#     - name: Set up Docker Buildx
#       uses: docker/setup-buildx-action@v3
    
#     - name: Log in to Docker Hub
#       uses: docker/login-action@v3
#       with:
#         username: ${{ secrets.DOCKER_USERNAME }}
#         password: ${{ secrets.DOCKER_PASSWORD }}
    
#     - name: Extract metadata for Django App
#       id: meta-django
#       uses: docker/metadata-action@v5
#       with:
#         images: ${{ env.REGISTRY }}/${{ env.DJANGO_IMAGE_NAME }}
#         tags: |
#           type=ref,event=branch
#           type=ref,event=pr
#           type=semver,pattern={{version}}
#           type=semver,pattern={{major}}.{{minor}}
#           type=sha,prefix={{branch}}-
#           type=raw,value=latest,enable={{is_default_branch}}
    
#     - name: Build and push Django App
#       uses: docker/build-push-action@v5
#       with:
#         context: .
#         file: ./Dockerfile
#         push: true
#         tags: ${{ steps.meta-django.outputs.tags }}
#         labels: ${{ steps.meta-django.outputs.labels }}
#         cache-from: type=gha
#         cache-to: type=gha,mode=max
    
#     - name: Extract metadata for Consumer
#       id: meta-consumer
#       uses: docker/metadata-action@v5
#       with:
#         images: ${{ env.REGISTRY }}/${{ env.CONSUMER_IMAGE_NAME }}
#         tags: |
#           type=ref,event=branch
#           type=ref,event=pr
#           type=semver,pattern={{version}}
#           type=semver,pattern={{major}}.{{minor}}
#           type=sha,prefix={{branch}}-
#           type=raw,value=latest,enable={{is_default_branch}}
    
#     - name: Build and push Consumer
#       uses: docker/build-push-action@v5
#       with:
#         context: .
#         file: ./test-execute-consumer/Dockerfile
#         push: true
#         tags: ${{ steps.meta-consumer.outputs.tags }}
#         labels: ${{ steps.meta-consumer.outputs.labels }}
#         cache-from: type=gha
#         cache-to: type=gha,mode=max
    
#     - name: Update Helm chart version
#       run: |
#         git config user.name github-actions
#         git config user.email github-actions@github.com
        
#         # Get the short SHA
#         SHORT_SHA=$(git rev-parse --short HEAD)
        
#         # Update values.yaml with new image tags
#         sed -i "s|tag: .*|tag: main-$SHORT_SHA|g" helm/ai-testops/values.yaml
        
#         # Commit and push if there are changes
#         if [[ -n $(git status -s) ]]; then
#           git add helm/ai-testops/values.yaml
#           git commit -m "chore: Update image tags to main-$SHORT_SHA [skip ci]"
#           git push
#         fi
#       env:
#         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

#   security-scan:
#     needs: build-and-push
#     runs-on: ubuntu-latest
#     if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4
    
#     - name: Run Trivy vulnerability scanner for Django App
#       uses: aquasecurity/trivy-action@master
#       with:
#         image-ref: ${{ env.REGISTRY }}/${{ env.DJANGO_IMAGE_NAME }}:latest
#         format: 'sarif'
#         output: 'trivy-django-results.sarif'
    
#     - name: Run Trivy vulnerability scanner for Consumer
#       uses: aquasecurity/trivy-action@master
#       with:
#         image-ref: ${{ env.REGISTRY }}/${{ env.CONSUMER_IMAGE_NAME }}:latest
#         format: 'sarif'
#         output: 'trivy-consumer-results.sarif'
