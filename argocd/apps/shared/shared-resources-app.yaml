apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ai-testops-shared
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: wide
  
  source:
    repoURL: https://huynhduc0.github.io/ai-testops
    chart: testops-infra
    targetRevision: "1.*.*"
    helm:
      values: |
        # Enable only shared resources
        postgres:
          enabled: false
        kafka:
          enabled: false
        ingress:
          enabled: true
        rbac:
          enabled: true
  
  destination:
    server: https://kubernetes.default.svc
    namespace: testops
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - SyncTimeout=1200
      - SkipDryRunOnMissingResource=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 20m
  
  ignoreDifferences:
    - group: ""
      kind: Secret
      jsonPointers:
        - /data
        - /stringData
