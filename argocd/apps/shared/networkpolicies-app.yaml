apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ai-testops-networkpolicies
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  project: wide
  
  source:
    repoURL: https://huynhduc0.github.io/ai-testops
    chart: ai-testops
    targetRevision: "1.*.*"
    helm:
      values: |
        # Enable only NetworkPolicies
        postgres:
          enabled: false
        kafka:
          enabled: false
        djangoApp:
          enabled: false
        consumer:
          enabled: false
        kafkaUI:
          enabled: false
        ingress:
          enabled: false
        keda:
          enabled: false
        rbac:
          enabled: false
        networkPolicies:
          enabled: true
  
  destination:
    server: https://kubernetes.default.svc
    namespace: testops
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - SyncTimeout=1200
      - SkipDryRunOnMissingResource=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 20m

